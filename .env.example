# === OBRIGATÓRIO ===
# Salt para HMAC-SHA256 de CPFs. Gerar com: python -c "import secrets; print(secrets.token_hex(32))"
# NUNCA commitar o valor real. Tratar como credencial de produção.
CPF_HMAC_SALT=TROCAR_POR_VALOR_SECRETO

# === API ===
DUCKDB_PATH=./pipeline/data/output/testa_de_ferro.duckdb
API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=4
API_RATE_LIMIT_PER_MINUTE=60
API_DEBUG=false

# === API Key (opcional) ===
# Chave para acesso bulk com rate limit maior. Deixar vazio para desabilitar.
API_BULK_KEY=

# === Frontend ===
VITE_API_BASE_URL=http://localhost:8000/api
