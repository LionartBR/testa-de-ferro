import { describe, it } from "vitest";

describe("Ranking", () => {
  it.todo("renders ranking table with fornecedores sorted by score");
  it.todo("filters by faixa de risco client-side");
  it.todo("paginates through results");
  it.todo("shows loading and error states");
});
