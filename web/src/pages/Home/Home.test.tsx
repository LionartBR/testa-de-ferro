import { describe, it } from "vitest";

describe("Home", () => {
  it.todo("renders stats summary when API returns data");
  it.todo("renders alert feed with pagination");
  it.todo("shows loading state while fetching");
  it.todo("shows error state on API failure");
});
