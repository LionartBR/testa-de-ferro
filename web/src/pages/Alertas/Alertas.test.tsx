import { describe, it } from "vitest";

describe("Alertas", () => {
  it.todo("renders alert feed with tipo and severidade filters");
  it.todo("filters by tipo via API (server-side)");
  it.todo("filters by severidade client-side");
  it.todo("paginates through alerts");
});
