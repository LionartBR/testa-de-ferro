import { describe, it } from "vitest";

describe("Busca", () => {
  it.todo("renders search input and updates URL params");
  it.todo("debounces search and fetches results after 300ms");
  it.todo("shows results as cards with CNPJ links");
  it.todo("stays idle when query is less than 2 characters");
});
